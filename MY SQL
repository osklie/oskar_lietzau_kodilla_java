
CREATE TABLE ISSUESLISTS
(
    ID SERIAL PRIMARY KEY,
    NAME  VARCHAR(100)
);

CREATE TABLE ISSUES
(
    ID SERIAL PRIMARY KEY,
    ISSUESLIST_ID BIGINT UNSIGNED NOT NULL,
    SUMMARY VARCHAR(100),
    DESCRIPTION VARCHAR(100),
    USER_ID_ASSIGNEDTO BIGINT UNSIGNED not null,
    FOREIGN KEY (USER_ID_ASSIGNEDTO) REFERENCES USERS(FIRSTNAME, LASTNAME)
);

SELECT * FROM ISSUES;

INSERT INTO ISSUESLISTS (ID, NAME)
VALUES (1, 'ToDo');

INSERT INTO ISSUESLISTS(ID, NAME)
VALUES (2, 'In progress');

INSERT INTO ISSUESLISTS(ID, NAME)
VALUES (3, 'Done');

SELECT * FROM ISSUES;

SELECT * FROM USERS;

INSERT INTO ISSUES(ID, ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)
VALUES (1, 1, 1, 'WRITE INSTRUCTIONS', 1);

INSERT INTO ISSUES(ID, ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)
VALUES (2, 2, 'DIGGING', 'Digging a ditch', 2);

INSERT INTO ISSUES(ID, ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)
VALUES (3, 2, 'RAKING', 'RAKING LEAVES', 3);

INSERT INTO ISSUES(ID, ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)
VALUES (4, 3, 'SWEEP', 'SWEEP the pavement', 4);

INSERT INTO ISSUES(ID, ISSUESLIST_ID, SUMMARY, DESCRIPTION, USER_ID_ASSIGNEDTO)
VALUES (5, 1, 'REPLACEMENT', 'REPLACEMENT OF THE BULB IN LAPMS', 5)
;


COMMIT;

___________________________________________________________________________;

SELECT I.SUMMARY, T.NAME
FROM ISSUES I
JOIN ISSUESLISTS T;

SELECT I.SUMMARY, U.LASTNAME
FROM ISSUES I
JOIN USERS U;

SELECT  U.FIRSTNAME, U.LASTNAME, I.SUMMARY
FROM USERS U
JOIN ISSUES I;

COMMIT;
